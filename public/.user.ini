; This host's OPcache doesn't pick up changed files by default, so
; `php artisan route:cache`/`config:cache` on deploy can silently keep
; serving stale bytecode until PHP is restarted. Force a timestamp check
; on every request so deploys take effect immediately.
opcache.validate_timestamps=1
opcache.revalidate_freq=0
